/**
 * NotificationContainer 组件
 *
 * 注意：当前项目使用 goey-toast 作为通知引擎，该组件保留为占位符
 * 以便未来需要自定义通知 UI 时可以扩展。
 *
 * 当前实现：返回 null，不渲染任何内容（通知由 goey-toast 处理）
 */

export function NotificationContainer() {
  // goey-toast 处理所有通知渲染，此组件作为兼容性占位符
  return null;
}
